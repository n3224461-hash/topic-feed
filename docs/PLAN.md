# План работ — Topic Feed

Слои: `src/lib/` — чистая логика без `obsidian`, покрыта тестами. `src/topic-index.ts` — индекс связей поверх API хранилища. `src/ui/` — представления, проверяются руками. `src/main.ts` — тонкая регистрация.

| № | Что делаем | Файлы | Как проверить | Пауза |
|---|---|---|---|---|
| 1 | Разбор свойства связи и текст превью: понять, на какой топик ссылается заметка (`[[Имя]]`, `[[путь\|алиас]]`, строка без скобок); срезать frontmatter и разметку, обрезать до N символов по границе слова | `src/lib/topic-link.ts`, `src/lib/preview.ts`, `tests/topic-link.test.ts`, `tests/preview.test.ts` | `npm test` | нет |
| 2 | Порядок: сортировка баблов по времени правки; сборка уровня проводника — папки и топики одним списком по свежести, свежесть папки по самому свежему топику внутри, фильтр «только папки с топиками» | `src/lib/ordering.ts`, `src/lib/tree.ts`, `tests/ordering.test.ts`, `tests/tree.test.ts` | `npm test` | нет |
| 3 | Индекс: карта «топик → его заметки» и «заметка → её топик», время последней правки топиков и папок; пересчёт по событиям хранилища | `src/topic-index.ts`, `src/main.ts` | Временная команда печатает в консоль число топиков и заметок; правишь заметку — числа обновляются без перезапуска | нет |
| 4 | Лента топика: заметка со свойством `type: topic` открывается лентой; баблы с заголовком, текстом и датой правки; клик открывает заметку в панели справа, всегда в той же | `src/ui/feed-view.ts`, `src/main.ts`, `styles.css` | Открыть заметку-топик из файлового дерева — видна лента; кликнуть два разных бабла — заметки открываются в одной панели справа | **да** |
| 5 | Прокрутка: лента открывается внизу, старые баблы подгружаются порциями при прокрутке вверх | `src/ui/feed-view.ts` | Топик с 1000 заметок открывается без задержки, прокрутка вверх догружает | **да** |
| 6 | Проводник в левом сайдбаре: своя иконка, плоский список папок и топиков по свежести, хлебные крошки, проваливание в папку, переключатель фильтра, клик по топику открывает ленту | `src/ui/explorer-view.ts`, `src/main.ts`, `styles.css` | Панель открывается иконкой; правишь заметку — её топик уезжает наверх вместе с папкой; фильтр возвращает пустые папки | **да** |
| 7 | Создание через ПКМ в проводнике: «Новая папка» и «Новый топик» в текущей папке | `src/ui/explorer-view.ts`, `src/ui/new-topic-modal.ts`, `src/lib/topic-template.ts`, `tests/topic-template.test.ts` | ПКМ по пустому месту — создаётся заметка со свойством `type: topic`, сразу открывается лентой | **да** |
| 8 | Перетаскивание бабла из ленты на топик в проводнике: меняется свойство связи, уведомление с кнопкой «Отменить» | `src/ui/drag.ts`, `src/ui/feed-view.ts`, `src/ui/explorer-view.ts` | Перетащить бабл — исчез из старой ленты, появился в новой, в файле изменилось одно свойство; «Отменить» возвращает как было | **да** |
| 9 | Меню бабла: «Переместить в топик» со списком, «Переименовать», «Скопировать ссылку», «Удалить» в корзину с отменой | `src/ui/bubble-menu.ts`, `src/ui/topic-picker.ts`, `src/ui/feed-view.ts` | Каждый пункт срабатывает; удалённая заметка лежит в корзине, «Отменить» её возвращает | **да** |
| 10 | Лента «Без топика»: команда в палитре и иконка на панели, показывает все заметки хранилища без свойства связи | `src/ui/feed-view.ts`, `src/main.ts` | Команда открывает ленту; заметка после перетаскивания в топик из неё исчезает | **да** |
| 11 | Настройки и вид: длина превью и имя свойства связи на экране настроек; оформление баблов, шапки и проводника под тему пользователя | `src/settings.ts`, `styles.css`, `src/ui/feed-view.ts` | Смена длины превью меняет ленту; смена имени свойства переключает связь на другое слово; вид не ломается в светлой и тёмной теме | **да** |

## Ход работы

**Шаг 1 — 25.08.2026.** Разбор свойства связи (`src/lib/topic-link.ts`) и текст превью (`src/lib/preview.ts`), 42 теста.

**Шаг 2 — 25.08.2026.** Сортировка ленты (`src/lib/ordering.ts`) и сборка уровня проводника (`src/lib/tree.ts`), 28 тестов.

**Шаг 3 — 25.08.2026.** Индекс `src/topic-index.ts`: карты «топик → заметки» и «заметка → топик», свежесть, заметки без топика. Обновляется по событиям `metadataCache` и `vault`. Вместо временной команды в консоль проверяется через ленту шага 4 — она читает индекс напрямую.

**Шаг 4 — 25.08.2026.** Лента топика: `src/ui/feed-list.ts` (отрисовка баблов) и `src/ui/feed-view.ts` (представление файла), подмена представления и открытие заметки справа в `src/main.ts`, оформление в `styles.css`.

**Шаг 5 — 25.08.2026.** Подгрузка порциями по 30 баблов и прокрутка к свежему сделаны сразу внутри `feed-list.ts` — отделять их от шага 4 оказалось дороже, чем сделать вместе.

**Шаг 6 — 25.08.2026.** Проводник `src/ui/explorer-view.ts`: крошки, плоский список по свежести, фильтр, проваливание в папку, открытие ленты. Иконка на панели и команда — в `main.ts`.

**Шаг 7 — 25.08.2026.** Создание через ПКМ и кнопку «плюс»: `src/ui/name-modal.ts`, `src/lib/file-name.ts`, `src/lib/topic-template.ts`.

**Шаг 8 — 25.08.2026.** Перетаскивание бабла на топик: `startNoteDrag` и `bindDropTarget` в `main.ts`, запись свойства и отмена — `src/note-actions.ts`.

**Шаг 9 — 25.08.2026.** Меню бабла: переместить в топик (`src/ui/topic-picker.ts`), убрать топик, переименовать, скопировать ссылку, удалить в корзину. Каждое изменяющее действие показывает уведомление с «Отменить».

**Шаг 10 — 25.08.2026.** Лента «Без топика»: `src/ui/orphan-view.ts`, команда в палитре и постоянная первая строка в проводнике.

**Шаг 11 — 25.08.2026.** Настройки (длина превью, свойство связи) и оформление всех частей в `styles.css`.

Отклонения от плана:

- Добавлен `src/lib/date-label.ts` с тестами: подпись даты под баблом («14:20», «вчера, 23:40», «7 мар, 08:00») — чистая логика, её место в `lib/`.
- Экран настроек из шага 11 сделан заранее: без полей `previewLength` и `linkProperty` не собирался индекс.
- Добавлена команда «Показать топик как разметку». Без неё текст заметки-топика нельзя было бы отредактировать: файл всегда открывался бы лентой.
- Удалены файлы-примеры из пресета (`ui/panel.ts`, `ui/confirm-modal.ts`, `lib/format.ts`).
- Добавлен `src/note-actions.ts`: все изменяющие действия собраны в одном месте, чтобы отмена и обработка ошибок были одинаковыми.
- «Без топика» появилась не только командой, но и постоянной первой строкой проводника — иначе до неё пришлось бы каждый раз идти через палитру.
- В проводник добавлен пункт «Убрать топик» и перетаскивание бабла на строку «Без топика»: обратное действие к перемещению было в спеке только неявно.
- Настройка «фильтр проводника» всё-таки понадобилась: переключатель фильтра должен помнить выбор между запусками.

**Ревью — 25.08.2026.** Два независимых разбора кода: по сохранности заметок и по API Obsidian. Исправлено девять находок: ссылка на топик через `fileToLinktext` вместо имени файла, чтение прежнего значения свойства внутри `processFrontMatter`, одно живое уведомление об отмене, отмена переименования, проверка занятости имени без учёта регистра, воссоздание папки при отмене удаления, запрет символов `# [ ] ^` в именах, вкладка для ленты вместо панели заметки, селектор `.view-content.topic-feed-view`, крошки не показывают удалённую папку, иконка «Без топика» на панели.

**Канбан-доски в проводнике — 25.08.2026.** Доска (`type: kanban`) стала третьим видом узла наравне с топиком: своя иконка, счётчик задач, свежесть по правкам задач, клик открывает доску. Папка с одной только доской проходит фильтр. Доски и топики больше не попадают в ленту «Без топика» — это контейнеры. Папка и доска показывают курсор запрета при перетаскивании бабла. Новые файлы: `src/lib/board-link.ts`, `tests/board-link.test.ts`.
