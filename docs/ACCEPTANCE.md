# Приёмка — Topic Feed

> Заполняется командой `/ship` по разделу «Готово, когда» из `SPEC.md`.

## Версия 0.1.0

| Критерий | Как проверяли | Результат | Дата |
|---|---|---|---|
| | | | |

## Автоматические проверки

- [ ] `npx tsc --noEmit` — без ошибок
- [ ] `npm test` — все тесты проходят
- [ ] `npm run build` — `main.js` собран

## Перед публикацией

- [ ] `manifest.json`: `author` и `authorUrl` заполнены, `id` совпадает с именем папки
- [ ] `README.md` описывает плагин, а не шаблон
- [ ] `LICENSE`: год и имя на месте
- [ ] В репозитории нет ключей, токенов и личных путей
- [ ] `CHANGELOG.md` описывает изменения языком пользователя
